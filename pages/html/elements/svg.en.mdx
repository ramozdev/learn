# SVG

## \<svg\>

The SVG element is an SVG graphics element which is used to draw graphics on a web page.

```html copy
<svg width="100" height="100">
  <circle cx="50" cy="50" r="40" stroke="green" stroke-width="4" fill="yellow" />
</svg>
```
